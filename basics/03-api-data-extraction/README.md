# API Data Extraction

Public API used: JSONPlaceholder Users endpoint.

Workflow:
1. Send a GET request.
2. Check the HTTP response.
3. Parse JSON.
4. Flatten nested objects with pandas.
5. Export the selected fields to CSV.

Before submission, run `api_assignment.ipynb` or `api_assignment.py` locally and capture a screenshot of the successful request and CSV output.
